[package]
name = "opengrid-planner"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Query planner for opengrid: pushdown, hybrid execution, ExecutionPlan"

[lints]
workspace = true

[dependencies]
opengrid-types = { path = "../opengrid-types" }
opengrid-query = { path = "../opengrid-query" }
opengrid-datasource = { path = "../opengrid-datasource" }

[dev-dependencies]
serde_json = "1"
